#pragma once

/*
#include "portal.h"
#include "chunk.h"

class PortalManager {
	
	Chunk current_chunk; //current chunk the player is in
	Portal portal; //this will hold the current portal setup dependent on player position

	PortalManager() = default;

};

*/